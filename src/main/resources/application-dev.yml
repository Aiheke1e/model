spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/gemify_test?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: gemify
    password: gemify
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      username: redis
      password: redis

logging:
  level:
    com.gemify: debug
